# 作業ログ 2026-03-12

## 実施内容

### 1. Vercelへのデプロイ
- Vercel CLIをインストールし、本番環境にデプロイ
- デプロイURL: https://business-card-app-phi.vercel.app
- 初回デプロイ時に404エラーが発生したが、時間経過で解消

### 2. GitHub CLIセットアップ & リポジトリ作成
- GitHub CLI (`gh`) をwingetでインストール
- ブラウザ認証でGitHubにログイン（67okamami@gmail.com）
- GitHubリポジトリを作成し、コードをプッシュ

### 3. Firebase Firestoreバックエンドへの移行
- `feature/firebase-backend` ブランチを作成
- Firebase CLIでログイン（Gemini連携はオフ）
- Firebase MCPサーバー経由でプロジェクト作成
  - プロジェクトID: `business-card-manager-app`
  - 表示名: Business Card Manager
- Firestoreデータベース初期化（asia-northeast1リージョン）
- Webアプリ登録 & SDK設定取得
- 実装変更:
  - `firebase` パッケージをインストール
  - `src/lib/firebase.ts` を新規作成（Firebase初期化設定）
  - `src/lib/storage.ts` をlocalStorage → Firestore CRUDに全面書き換え
  - 全ストレージ関数を非同期化（Promise対応）
  - `src/app/page.tsx` - loadCardsをasync化
  - `src/app/cards/[id]/page.tsx` - getCardById/deleteCardをawait対応
  - `src/app/cards/[id]/edit/page.tsx` - getCardByIdをawait対応
  - `src/components/business-card-form.tsx` - saveCard/updateCardをawait対応、保存中ローディング追加
- Firestoreセキュリティルールをデプロイ（全読み書き許可）
- Firebase Storage連携は一旦スキップ（画像はdata URLのままFirestoreに保存）
- `feature/firebase-backend` → `master` にマージ（Fast-forward）
- Vercelに再デプロイ完了

## コミット履歴
- `c566d52` feat: localStorageからFirebase Firestoreバックエンドに移行
- `780bfbe` Initial commit: 名刺管理Webアプリ

## 残課題
- Firebase Storageの有効化（名刺画像の保存先をdata URLからStorage URLに変更）
- Firestoreセキュリティルールの強化（現在は全開放状態）
- OCR技術スタックの再検討（Tesseract.js + regex → Claude Vision API等への移行を検討中）
- 実装計画書.mdの更新（Firebase移行内容を反映）
